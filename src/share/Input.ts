import styled from 'styled-components';

export const Input = styled.input`
    border: none;
    outline: none;
    background-color: var(--grey-color);
    padding: 10px 20px;
    border-radius: 5px;
`